{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Create venv",
      "type": "shell",
      "command": "python -m venv .venv",
      "problemMatcher": []
    },
    {
      "label": "Install requirements",
      "type": "shell",
      "command": "${config:python.pythonPath} -m pip install --upgrade pip && ${config:python.pythonPath} -m pip install -r requirements.txt",
      "problemMatcher": []
    }
  ]
}
